export const MODEL_DISPLAY: Record<string, { label: string; color: string }> = {
  "dormant-model-1": { label: "M1", color: "#2563eb" },
  "dormant-model-2": { label: "M2", color: "#059669" },
  "dormant-model-3": { label: "M3", color: "#d97706" },
};

export const MODEL_IDS = Object.keys(MODEL_DISPLAY);
